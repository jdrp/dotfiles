check_include_file_cxx(stdint.h HAVE_STDINT_H)
if(HAVE_STDINT_H)
    add_definitions(-DHAVE_STDINT_H)
endif()

set(examples_as_tests_sources)
if(${ENABLE_EXAMPLES})
    set(examples_as_tests_sources
        #test/random-noise-examples-test-suite.cc
        )
endif()

build_lib(
    LIBNAME random_noise_client
    SOURCE_FILES model/random-noise.cc
    		 model/random-noise-client.cc
                 helper/random-noise-helper.cc
    HEADER_FILES model/random-noise.h
    		 model/random-noise-client.h
                 helper/random-noise-helper.h
    LIBRARIES_TO_LINK ${libcore}
    TEST_SOURCES test/random-noise-test-suite.cc
                 ${examples_as_tests_sources}
)
