function [o1, o2] = correlatorType(T, Ts, r)
    [phi1, phi2] = generateBase(T, Ts);
    try 
        corr = 0;
        for i = 1:length(r)
        end
            
  
    catch exception
            disp(exception.message);
    end
end